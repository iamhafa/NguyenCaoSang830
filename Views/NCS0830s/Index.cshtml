@model IEnumerable<NguyenCaoSang830.Models.NCS0830>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NCSName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NCSGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NCSName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NCSGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NCSId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NCSId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NCSId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
